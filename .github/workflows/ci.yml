name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: gcc -o app src/main.c && echo 'Build OK'
      - run: gcc -o test_app tests/test_main.c && ./test_app
